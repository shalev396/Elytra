# GitHub → Settings → Secrets and variables → Actions. Locally, copy to server/.env.<stage>.

# Repository secrets
AWS_ACCOUNT_ID="123456789012"

# Repository variables
AWS_ROLE_NAME="github-actions-deploy"
AWS_REGION="us-east-1" # optional

# Environment secrets (dev, qa, prod)
DOMAIN_NAME="dev.example.com"
DATABASE_URL="postgres://user:password@host:5432/db"
CERTIFICATE_ARN="" # optional
WAF_WEB_ACL_ARN="" # optional

# Environment variables (dev, qa, prod): none
DOMAIN_NAME="dev.example.com"